function add(){
	
	if(frm.sid.valuse.length == 0){
		alert('학번 입력 하세요');
		frm.sid.focus();
		return false;
	}
	if(frm.sname.valuse.length == 0){
		alert('학번 입력 하세요');
		frm.sid.focus();
		return false;
	}
	if(frm.sid.valuse.length == 0){
		alert('학번 입력 하세요');
		frm.sid.focus();
		return false;
	}
	if(frm.sid.valuse.length == 0){
		alert('학번 입력 하세요');
		frm.sid.focus();
		return false;
	}
	if(frm.sid.valuse.length == 0){
		alert('학번 입력 하세요');
		frm.sid.focus();
		return false;
	}
	if(frm.sid.valuse.length == 0){
		alert('학번 입력 하세요');
		frm.sid.focus();
		return false;
	}





}